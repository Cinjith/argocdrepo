apiVersion: apps/v1
kind: Deployment
metadata:
  name: dep-cb
  namespace: cinjith
  annotations:
    kubernetes.io/change-cause: "Revision two"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web1
  template:
    metadata:
      labels:
        app: web1
    spec:
      containers:
      - name: c1
        image: nginxdemos/hello

